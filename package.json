{
  "name": "build.haxe.org",
  "version": "0.0.0",
  "description": "Haxe Foundation snapshot build storage web UI.",
  "main": "index.js",
  "scripts": {
    "postinstall": "haxelib newrepo && haxelib dev hxnodelibs libs/hxnodelibs && haxelib install hxnodejs",
    "build": "haxe build.hxml",
    "start": "nodemon --watch index.js index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "dotenv sls deploy"
  },
  "author": "Haxe Foundation",
  "license": "CC0-1.0",
  "dependencies": {
    "aws-sdk": "^2.500.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "serverless-http": "^2.0.2"
  },
  "devDependencies": {
    "dotenv-cli": "^2.0.1",
    "nodemon": "^1.19.1",
    "serverless": "^1.49.0"
  }
}
